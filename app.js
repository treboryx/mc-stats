const util = require("./util.js");

// the below functions serves a testing purpose - uncomment to see what stats are displayed for each server
const main = async () => {
  // util.blocksmc("_IPlozS").then(R => console.log(r));
  // util.funcraft("nathan818").then(r => console.log(r));
  // util.mineplex("xeiu").then(r => console.log(r));
  // util.manacube("KaiYara12").then(r => console.log(r));
  // util.minesaga("doitnowornever").then(r => console.log(r));
};

main();